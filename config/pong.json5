{
  mode: "peer",
  listen: {
    endpoints: ["unixpipe/example-pipe"]
  },
  transport: {
    unicast: {
      lowlatency: true,
      qos: {
        enabled: false,
      },
    },
    shared_memory: {
      enabled: true,
    },
  }
}
